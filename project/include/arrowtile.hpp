#pragma once
#include "sprite.hpp"

/*

enum Directions { UP = 0, RIGHT, DOWN, LEFT };
class ArrowTile : public AnimatedSprite {
    private:
        static bool seeded_;
        float  rotation_ = 0;

    public:
        enum Directions direction = UP;
        ArrowTile(Vector2 position, Texture2D* spriteSheet, float frameHeight, short frameCount, float frameTimeSecond, bool loop = NOT_LOOPABLE);
        void decideDirection();
};

*/